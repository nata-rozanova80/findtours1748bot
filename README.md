# Telegram бот-парсер горящих туров

Телеграм-бот, который парсит спецпредложения («горящие туры») с открытых HTML-страниц туроператоров Tez-tour и 1001-tours, сохраняет их в SQLite и по команде `/promo` отправляет пользователю до 10 актуальных предложений с заголовками и ссылками.

## Возможности бота

- Парсинг горящих предложений с сайтов Tez-tour и 1001-tours.
- Сохранение спецпредложений в локальную базу SQLite.
- Команда `/promo` в Telegram возвращает до 10 последних предложений из базы.
- Команда `/start` показывает приветствие и краткую инструкцию.
- Команда `/about` выводит краткую информацию о боте и источниках туров.
- Команда `/help` показывает список доступных команд и их назначение.
- Команда `/update` запускает парсер и обновляет базу туров.
- Команда `/stats` выводит простую статистику по базе туров (количество предложений по последнему выбору).

## Технологический стек

- Язык: **Python 3.x**
- Telegram: `python-telegram-bot` (рекомендуемая версия 20.8)
- HTTP: `requests`
- Парсинг HTML: `BeautifulSoup4`
- База данных: `sqlite3`, локальный файл (например, `offers.db`)
- Конфигурация: `.env` (TOKEN и др.) через `python-dotenv`
- Логирование: стандартный модуль `logging`

Пример минимального `requirements.txt`:

```txt
anyio==4.12.1
beautifulsoup4==4.14.3
certifi==2026.1.4
charset-normalizer==3.4.4
dotenv==0.9.9
h11==0.16.0
httpcore==1.0.9
httpx==0.26.0
idna==3.11
python-dotenv==1.2.1
python-telegram-bot==20.8
requests==2.32.5
sniffio==1.3.1
soupsieve==2.8.3
typing_extensions==4.15.0
urllib3==2.6.3
```

## Архитектура проекта

Рекомендуемая структура файлов:

 `bot.py` — Telegram-бот, обработчики команд `/start`, `/promo`, `/about`, `/help`, `/update`, `/stats`, запуск приложения.
- `parser.py` — функции парсинга для каждого сайта:
  - `get_offers_from_tez()`
  - `get_offers_from_1001tur()`
- `db.py` — работа с SQLite: инициализация базы, сохранение, выборка данных
- `config.py` — загрузка конфигурации и переменных окружения из `.env`
- `utils.py` — вспомогательные функции (например, нормализация данных, удаление дублей)
- `update_offers.py` - отдельный скрипт для ручного обновления
- `requirements.txt` — список зависимостей проекта
- `.env` — токен бота 


### Структура базы данных

Минимальная таблица `offers`:

```sql
CREATE TABLE IF NOT EXISTS offers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    link TEXT NOT NULL,
    source TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(title, link)
);
```

**Описание полей:**

- `id` — уникальный идентификатор записи
- `title` — название тура или акции
- `link` — ссылка на страницу предложения
- `source` — идентификатор туроператора (`coral`, `anex`, `sunmar` и т.д.)
- `created_at` — дата и время добавления записи
- `UNIQUE(title, link)` — защита от дублирования одинаковых предложений

**Основные функции в `db.py`:**

- `init_db()` — создание таблицы при первом запуске
- `save_offers(offers: list[dict])` — сохранение списка предложений с автоматической обработкой дублей
- `get_last_offers(limit: int = 10)` — получение последних N предложений для выдачи по команде `/promo`

## Логика Telegram-бота

### Команды

#### **`/start`** — приветствие и краткая инструкция по использованию бота
Пример ответа:

```
Привет! Я бот для поиска горящих туров 🔥

Я отслеживаю спецпредложения от популярных туроператоров 
и помогу тебе найти выгодные туры.

Команды:
/promo — показать топ-10 актуальных предложений
/start — показать это сообщение

Удачного путешествия! ✈️
```

#### **`/promo`** — вывод до 10 последних предложений из базы данных

Пример ответа:

```
🔥 Топ горящих туров на сейчас:

1. Турция: горящие туры из Москвы со скидкой до 30%
   https://www.coral.ru/offer1

2. Египет: спецпредложения на февраль 2026
   https://anextour.ru/offer2

3. ОАЭ: раннее бронирование с выгодой до 40%
   https://www.sunmar.ru/offer3

...

10. Вьетнам: пляжный отдых от 45 000 руб
    https://www.coral.ru/offer10
```

Если предложений нет в базе:

```
К сожалению, сейчас нет актуальных предложений.
Попробуйте позже или используйте /promo для повторной проверки.
```

#### **`/update`** - сообщает пользователю, что запускается обновление базы туров.

Вызывает update_offers_from_all_sources().

По завершении сообщает, сколько новых предложений добавлено:

```
Обновление завершено. Добавлено новых предложений: N.
```

При ошибке логирует её и отвечает:

```
Произошла ошибка при обновлении базы. Попробуйте позже.
```

#### **`/stats`** - Временная реализация статистики: 

Получает данные через get_last_offers(limit=1000) и считает количество записей.
Отправляет текст вида:

```
Статистика по базе туров:
Всего предложений (по последнему выбору): N
Подробная статистика по источникам будет добавлена позже.
При ошибке сообщает, что получить статистику не удалось.
```

### Поток данных

1. **Парсер** забирает данные с выбранных сайтов туроператоров
2. Результаты парсинга сохраняются в SQLite через функцию `save_offers()`
3. Пользователь отправляет команду `/promo` боту
4. Бот читает последние записи через `get_last_offers(limit=10)`
5. Формируется и отправляется ответное сообщение пользователю

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd <ПАПКА_ПРОЕКТА>
```

### 2. Создание виртуального окружения и установка зависимостей

**Windows:**

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**Linux/macOS:**

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Создание файла `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather

# Опционально: настройки прокси (если требуется)
# PROXY_URL=socks5://user:pass@host:port
```

**Как получить токен:**

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (укажите имя и username бота)
4. Скопируйте полученный токен в файл `.env`

### 4. Инициализация базы данных

Обычно функция `init_db()` вызывается автоматически при первом запуске `bot.py`.

Если используется отдельный скрипт инициализации:

```bash
python init_db.py
```

### 5. Запуск бота

```bash
python bot.py
```

После успешного запуска в консоли появится сообщение:

```
INFO: Бот запущен и готов к работе...
```

Теперь бот доступен в Telegram и готов принимать команды `/start` и `/promo`.

## Обновление предложений (расписание)

Существует два основных подхода к регулярному обновлению базы данных:

### Вариант 1: Внешний планировщик (рекомендуется для учебного проекта)

**Cron (Linux/macOS):**

Добавьте задачу в crontab (запуск каждый час):

```bash
0 * * * * /путь/к/venv/bin/python /путь/к/проекту/parser_runner.py
```

**Task Scheduler (Windows):**

1. Откройте «Планировщик заданий»
2. Создайте новую задачу
3. Укажите триггер (например, каждый час)
4. Действие: запуск `python.exe` с аргументом `parser_runner.py`

### Вариант 2: Встроенный планировщик (APScheduler)

Добавьте в `bot.py`:

```python
from apscheduler.schedulers.background import BackgroundScheduler

def update_offers_job():
    """Фоновая задача обновления предложений"""
    logging.info("Запуск обновления предложений...")
    # Вызов функций парсинга
    offers = []
    offers.extend(get_offers_from_coral())
    offers.extend(get_offers_from_anex())
    offers.extend(get_offers_from_sunmar())
    
    save_offers(offers)
    logging.info(f"Обновление завершено. Добавлено/обновлено: {len(offers)} предложений")

# Настройка планировщика
scheduler = BackgroundScheduler()
scheduler.add_job(update_offers_job, 'interval', hours=1)  # Каждый час
scheduler.start()
```