# Telegram бот-парсер горящих туров

Телеграм-бот, который парсит спецпредложения («горящие туры») с открытых HTML-страниц туроператоров, сохраняет их в SQLite и по команде `/promo` отправляет пользователю до 10 актуальных предложений с заголовками и ссылками.

## Возможности бота

- Парсинг горящих предложений с 1–3 сайтов туроператоров (Coral Travel, ANEX Tour, Sunmar и др.)
- Извлечение из HTML названия акции/тура и ссылки на страницу предложения
- Сохранение результатов парсинга в локальную базу SQLite (`offers.db`)
- Команда `/promo` в Telegram возвращает до 10 последних предложений из базы
- Команда `/start` показывает приветствие и краткую справку по использованию бота
- Возможность расширения: добавление новых источников, полей (источник, дата создания, цена и т.п.), планировщика для регулярного обновления

## Технологический стек

- Язык: **Python 3.x**
- Telegram: `python-telegram-bot` (рекомендуемая версия 20.8)
- HTTP: `requests`
- Парсинг HTML: `BeautifulSoup4`
- База данных: `sqlite3`, локальный файл `offers.db`
- Конфигурация: `.env` (TOKEN и др.) через `python-dotenv`

Пример `requirements.txt` (можно сократить до реально используемых библиотек):

```txt
anyio==4.12.1
APScheduler==3.6.3
asyncio==4.0.0
cachetools==4.2.2
certifi==2026.1.4
charset-normalizer==3.4.4
class-registry==2.1.2
contourpy==1.3.3
cycler==0.12.1
filters==1.3.2
fonttools==4.61.1
h11==0.16.0
httpcore==1.0.9
httpx==0.26.0
idna==3.11
kiwisolver==1.4.9
matplotlib==3.10.8
nest-asyncio==1.6.0
numpy==2.4.1
packaging==26.0
pandas==3.0.0
pillow==12.1.0
pyparsing==3.3.2
PySocks==1.7.1
python-dateutil==2.9.0.post0
python-dotenv==1.0.0
python-telegram-bot==20.8
pytz==2025.2
regex==2026.1.15
requests==2.31.0
setuptools==80.10.1
six==1.17.0
sniffio==1.3.1
tornado==6.1
typing_extensions==4.15.0
tzdata==2025.3
tzlocal==5.3.1
urllib3==2.6.3
```

## Архитектура проекта

Рекомендуемая структура файлов:

- `bot.py` — Telegram-бот, обработчики команд `/start` и `/promo`, запуск приложения
- `parser.py` — функции парсинга для каждого сайта: `get_offers_from_coral()`, `get_offers_from_anex()`, `get_offers_from_sunmar()` и т.п.
- `db.py` — работа с SQLite: инициализация базы, сохранение, выборка данных
- `config.py` — загрузка конфигурации и переменных окружения из `.env`
- `utils.py` — вспомогательные функции (например, нормализация данных, удаление дублей)
- `requirements.txt` — список зависимостей проекта
- `.env` — токен бота и другие секреты (не коммитить в репозиторий)

### Структура базы данных

Минимальная таблица `offers`:

```sql
CREATE TABLE IF NOT EXISTS offers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    link TEXT NOT NULL,
    source TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(title, link)
);
```

**Описание полей:**

- `id` — уникальный идентификатор записи
- `title` — название тура или акции
- `link` — ссылка на страницу предложения
- `source` — идентификатор туроператора (`coral`, `anex`, `sunmar` и т.д.)
- `created_at` — дата и время добавления записи
- `UNIQUE(title, link)` — защита от дублирования одинаковых предложений

**Основные функции в `db.py`:**

- `init_db()` — создание таблицы при первом запуске
- `save_offers(offers: list[dict])` — сохранение списка предложений с автоматической обработкой дублей
- `get_last_offers(limit: int = 10)` — получение последних N предложений для выдачи по команде `/promo`

## Парсинг сайтов

Бот работает только с открытыми HTML-страницами туроператоров без использования закрытых API.

**Примеры целевых URL:**

- Coral Travel: `https://www.coral.ru/poleznaya-informatsiya/offers/hot-offers/`
- ANEX Tour: `https://anextour.ru/actions`
- Sunmar: `https://www.sunmar.ru/info-actions/`

Для каждого сайта в `parser.py` реализуется отдельная функция:

```python
def get_offers_from_coral() -> list[dict]:
    """
    Парсит страницу Coral Travel и возвращает список офферов.
    
    Возвращает:
        [
            {"title": "Горящие туры в Турцию", "link": "https://...", "source": "coral"},
            {"title": "Спецпредложения в Египет", "link": "https://...", "source": "coral"},
            ...
        ]
    """
    try:
        response = requests.get(url, timeout=10)
        response.raise_for_status()
        soup = BeautifulSoup(response.text, 'html.parser')
        
        # Подбор селекторов на основе реальной HTML-структуры
        offers = []
        # ... логика парсинга ...
        
        return offers
    except Exception as e:
        logging.error(f"Ошибка при парсинге Coral: {e}")
        return []
```

Аналогично реализуются `get_offers_from_anex()` и `get_offers_from_sunmar()`.

**Важные принципы:**

- Использовать устойчивые CSS-селекторы на основе предоставленных HTML-примеров
- При ошибках сети или изменении верстки функция должна логировать проблему и возвращать пустой список
- Не прерывать работу всего бота из-за недоступности одного источника

## Логика Telegram-бота

### Команды

**`/start`** — приветствие и краткая инструкция по использованию бота

Пример ответа:

```
Привет! Я бот для поиска горящих туров 🔥

Я отслеживаю спецпредложения от популярных туроператоров 
и помогу тебе найти выгодные туры.

Команды:
/promo — показать топ-10 актуальных предложений
/start — показать это сообщение

Удачного путешествия! ✈️
```

**`/promo`** — вывод до 10 последних предложений из базы данных

Пример ответа:

```
🔥 Топ горящих туров на сейчас:

1. Турция: горящие туры из Москвы со скидкой до 30%
   https://www.coral.ru/offer1

2. Египет: спецпредложения на февраль 2026
   https://anextour.ru/offer2

3. ОАЭ: раннее бронирование с выгодой до 40%
   https://www.sunmar.ru/offer3

...

10. Вьетнам: пляжный отдых от 45 000 руб
    https://www.coral.ru/offer10
```

Если предложений нет в базе:

```
К сожалению, сейчас нет актуальных предложений.
Попробуйте позже или используйте /promo для повторной проверки.
```

### Поток данных

1. **Парсер** забирает данные с выбранных сайтов туроператоров
2. Результаты парсинга сохраняются в SQLite через функцию `save_offers()`
3. Пользователь отправляет команду `/promo` боту
4. Бот читает последние записи через `get_last_offers(limit=10)`
5. Формируется и отправляется ответное сообщение пользователю

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd <ПАПКА_ПРОЕКТА>
```

### 2. Создание виртуального окружения и установка зависимостей

**Windows:**

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**Linux/macOS:**

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Создание файла `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather

# Опционально: настройки прокси (если требуется)
# PROXY_URL=socks5://user:pass@host:port
```

**Как получить токен:**

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (укажите имя и username бота)
4. Скопируйте полученный токен в файл `.env`

### 4. Инициализация базы данных

Обычно функция `init_db()` вызывается автоматически при первом запуске `bot.py`.

Если используется отдельный скрипт инициализации:

```bash
python init_db.py
```

### 5. Запуск бота

```bash
python bot.py
```

После успешного запуска в консоли появится сообщение:

```
INFO: Бот запущен и готов к работе...
```

Теперь бот доступен в Telegram и готов принимать команды `/start` и `/promo`.

## Обновление предложений (расписание)

Существует два основных подхода к регулярному обновлению базы данных:

### Вариант 1: Внешний планировщик (рекомендуется для учебного проекта)

**Cron (Linux/macOS):**

Добавьте задачу в crontab (запуск каждый час):

```bash
0 * * * * /путь/к/venv/bin/python /путь/к/проекту/parser_runner.py
```

**Task Scheduler (Windows):**

1. Откройте «Планировщик заданий»
2. Создайте новую задачу
3. Укажите триггер (например, каждый час)
4. Действие: запуск `python.exe` с аргументом `parser_runner.py`

### Вариант 2: Встроенный планировщик (APScheduler)

Добавьте в `bot.py`:

```python
from apscheduler.schedulers.background import BackgroundScheduler

def update_offers_job():
    """Фоновая задача обновления предложений"""
    logging.info("Запуск обновления предложений...")
    # Вызов функций парсинга
    offers = []
    offers.extend(get_offers_from_coral())
    offers.extend(get_offers_from_anex())
    offers.extend(get_offers_from_sunmar())
    
    save_offers(offers)
    logging.info(f"Обновление завершено. Добавлено/обновлено: {len(offers)} предложений")

# Настройка планировщика
scheduler = BackgroundScheduler()
scheduler.add_job(update_offers_job, 'interval', hours=1)  # Каждый час
scheduler.start()
```

Для учебного проекта достаточно реализовать любой один из вариантов.

## Логирование и обработка ошибок

Используйте модуль `logging` для отслеживания работы бота и диагностики проблем.

**Базовая настройка логирования:**

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler()
    ]
)
```

**Что нужно логировать:**

1. **HTTP-ошибки:**
   - Сайт не отвечает (timeout)
   - Коды ошибок 4xx/5xx
   - Проблемы с сетью

2. **Ошибки парсинга:**
   - Парсер не нашёл ни одного предложения (возможное изменение верстки)
   - Некорректная структура HTML

3. **Ошибки базы данных:**
   - Невозможность записать данные
   - Проблемы с чтением из БД
   - Дублирование записей

4. **Ошибки Telegram API:**
   - Неверный токен
   - Проблемы с подключением
   - Превышение лимитов запросов

**Пример обработки ошибок:**

```python
try:
    response = requests.get(url, timeout=10)
    response.raise_for_status()
except requests.Timeout:
    logging.error(f"Timeout при запросе к {url}")
    return []
except requests.HTTPError as e:
    logging.error(f"HTTP ошибка {e.response.status_code} при запросе к {url}")
    return []
except Exception as e:
    logging.error(f"Неожиданная ошибка при парсинге: {e}")
    return []
```

## Минимальные требования и расширения

### Обязательный минимум (для зачёта)

- ✅ Telegram-бот с командами `/start` и `/promo`
- ✅ Парсинг минимум одного сайта с открытой HTML-страницы
- ✅ Сохранение полей `id`, `title`, `link` в SQLite
- ✅ Выдача до 10 актуальных предложений по команде `/promo`

### Дополнительные улучшения (для портфолио)

- 🌟 Поддержка 2–3 сайтов-источников
- 🌟 Поля `source`, `created_at` в базе данных
- 🌟 Логика устранения дублей в БД (UNIQUE constraint)
- 🌟 Планировщик для регулярного автоматического обновления базы
- 🌟 Подробное логирование и обработка всех типов ошибок
- 🌟 Inline/Reply кнопки для быстрого запроса `/promo` без ручного ввода команды
- 🌟 Фильтрация предложений по направлениям или ценам
- 🌟 Уведомления пользователей о новых горящих турах

## Формат сдачи проекта

Для успешной сдачи проекта необходимо предоставить:

1. **Ссылка на бота в Telegram**
   - Username бота (например, @hot_tours_bot)
   - Бот должен быть запущен и доступен для тестирования

2. **Ссылка на репозиторий или архив проекта**
   - GitHub/GitLab с полным исходным кодом
   - Или ZIP-архив проекта

3. **Документация со скриншотами**
   - PDF или Word документ с демонстрацией работы:
     - Скриншот команды `/start` и ответа бота
     - Скриншот команды `/promo` с примером списка предложений
     - Опционально: скриншот структуры базы данных или логов обновления

4. **README.md** (этот файл) в корне проекта

5. **Файл requirements.txt** для быстрого развёртывания

**Не включайте в репозиторий:**

- ❌ Файл `.env` с токенами (добавьте в `.gitignore`)
- ❌ Файл базы данных `offers.db`
- ❌ Директорию `venv/`
- ❌ Файлы логов `*.log`

**Пример `.gitignore`:**

```
venv/
.env
*.db
*.log
__pycache__/
*.pyc
.idea/
.vscode/
```

## Контакты и поддержка

При возникновении вопросов или проблем:

- Проверьте логи в файле `bot.log`
- Убедитесь, что все зависимости установлены
- Проверьте корректность токена в `.env`
- Убедитесь, что целевые сайты доступны

---

**Удачи в разработке! 🚀**